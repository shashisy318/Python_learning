tup = (2, 3, 23, 45, 54, 23, 29, 68)

print(tup)

# count(value)

no = tup.count(23)
print(no)

# index(value)
 
print(tup.index(23))

# len(tup)
# search()

# you can search more methods using chatgpt

# Slicing in tuple is done as in list but changes are saved in other tuple 
